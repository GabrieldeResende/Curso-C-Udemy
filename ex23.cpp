#include<stdio.h>
#include<stdlib.h>
#include<math.h>
#include<conio.h>

int main(void) 
{
	int i, positivo=0, negativo=0;
	
	i=1;
	while (i != 0)
	{
		printf("digite um numero: ");
		scanf("%d", &i);
		positivo > 0;
		negativo < 0;
			if (i>0)
				positivo = positivo + i;
			else
				negativo = negativo + i;	
	}
		printf("a soma dos numeros positivos sao: %d \n", positivo);
		printf("a soma dos numeros negativos sao: %d \n", negativo);
}
